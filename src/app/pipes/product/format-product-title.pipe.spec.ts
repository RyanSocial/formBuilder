import { FormatProductTitlePipe } from './format-product-title.pipe';

describe('FormatProductTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatProductTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
